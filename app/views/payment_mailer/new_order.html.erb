
<h2 style="color:red">Bonjour!</h2>


<div style="background: green; padding: 30px">
<p><%= @order.user.full_name %> vient de passer commande.</p>


<p>Commande n° <%= @order.id %> pour laquelle on estime une livraison entre <%= l(@order.delivery_date_1,  format: '%d %B %Y' ) %> et <%= l(@order.delivery_date_2, format: '%d %B %Y') %></p>

<% @order.items.each do |item| %>
	<ul>
		<li> <%= item.quantity %> X <%=item.variant.product.title %>, <%= item.variant.product.brand %> , <%= item.variant.product.color  if item.variant.product.color %> %= item.variant.color  if item.variant.color %> , en taille <%= item.variant.size %> </li>
	</ul>
<% end %>


<p>Voir la commande en ligne <%= link_to "Commande", admin_orders_url %></p>

