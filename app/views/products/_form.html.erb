<% if current_user && current_user.admin? %>
	<%= form_for @product do |f| %>		
		<%= f.text_field :title, placeholder: "Titre " %>
		<%= f.text_field :price, placeholder: "Prix " %>
		<%= f.text_field :description, placeholder: "Description" %>
		<%= f.text_field :quantity, placeholder: "Nombre de pièces disponibles" %>
		<%= f.text_field :color, placeholder: "Couleur" %>
		<%= f.text_field :size, placeholder: "Taille" %>
		<%= f.collection_select :category_id, Category.all, :id, :title %>
		<%= f.submit "Ajouter" %>
	<% end %>
<% end %>
