Bonjour <%= Company.instance.admin_name %>

Le client <%= @returning.user.full_name %> a demandé un retour pour la marchandise suivante 

<% @returning.items.where(selected: true).each do |item| %>
	<%= item.quantity %>  - <%= item.variant.product.title %> - <%= item.variant.product.brand %> , en taille <%= item.variant.size  %>
<% end %>

Il doit vous l'envoyer au plus tard le <%= l(Date.today + 10.days,  format: '%d %B %Y' ) %>

L'équipe Damien