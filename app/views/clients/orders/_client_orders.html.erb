<% @client_orders.order('created_at DESC').each do |order| %>
	<%= link_to clients_order_path(order) do  %>
		<div class="small-cards">
			<div class="row">
				<div class="col-6">
					<p>Commandé le : </p><p><%= l(order.created_at, format: '%d %B %Y' )%></p>
				</div>
				<div class="col-4">
					<p>Articles: <%= order.sent_articles %></p>
					<p>Sous total : <%= number_to_currency_euro order.sub_total %></p>
				</div>
				<div class="col-2">
					<i class="fa fa-chevron-right"></i>
				</div>
			</div>		
		</div>
	<% end %>
<% end %>