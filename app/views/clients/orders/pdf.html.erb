<div class="container">
  
  <div class="row">
    <div class="offset-6 col-6">
        <h1>FACTURE</h1>
        
        <hr>
    </div>
  </div>

  <div class="row">
    <div class="col-6">
      <p><%= Company.instance.name %></p>
      <p><%= Company.instance.address_1 %></p>
      <p><%= Company.instance.address_2 %></p>
      <p><%= Company.instance.zipcode %> <%= Company.instance.city %></p>
      <p><%= Company.instance.country %></p>
    </div>

    <div class="col-3 offset-3">
        <% if order.user.billing_address.present? %>
          <p><%= order.user.billing_address.full_name %></p>
          <p><%= order.user.billing_address.street_number %> <%= order.user.billing_address.route %></p>
          <p><%= order.user.billing_address.postal_code %> <%= order.user.billing_address.locality %></p>
        <% else %>
          <p><%= order.user.shipping_address.full_name %></p>
          <p><%= order.user.shipping_address.street_number %> <%= order.user.shipping_address.route %></p>
          <p><%= order.user.shipping_address.postal_code %> <%= order.user.shipping_address.locality %></p>
        <% end %>
    </div>
  </div>


  <div class="row">
    <div class="col-4">
     <div class="reference_invoice">
       <p>Facture N° <%= order.id %></p>
       <p>Ref Client :  <%= order.user.id %></p>
       <p>Date : <%= l(order.created_at, format: '%d %B %Y' ) %></p>
     </div>
    </div>
  </div>


  <table class="table table-striped">
    <thead>
        <tr>
          <th>Quantité</th>
          <th>Désignation</th> 
          <th>Taille</th> 
          <th>Prix</th>
        </tr>
    </thead>
    <tbody>
      <% order.items.each do |item| %>
  
      <tr>
        <td><%= item.quantity %> </td>
        <td><%= item.variant.product.title %></td>
        <td><%= item.variant.size %></td>
        <td align="right"><%= number_to_currency(item.variant.product.price) %>€</td>
      </tr>

      <% end %>
    </tbody>
    <tfoot>
      <tr>
        <td colspan="3" align="right">Sous Total</td>
        <td colspan="3" align="right"><%= number_to_currency order.total %>€</td>
      </tr>
      <tr>
        <td colspan="3" align="right">Frais de port</td>
        <td colspan="3" align="right"><%= number_to_currency order.shipping_fees %>€</td>
      </tr>
      <tr>
        <td colspan="3" align="right">Total</td>
        <td colspan="3" align="right"><%= number_to_currency order.total %>€</td>
      </tr>
    </tfoot>
  </table>


  <div class="row">
    <div class="col-12">
      <small>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Quaerat dolor illo repellat repudiandae nisi quod provident dolorem nobis temporibus earum, voluptatum magni obcaecati eveniet necessitatibus sed aperiam eum ut esse.</small>
    </div>
  </div>

</div>