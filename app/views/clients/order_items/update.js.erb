$('.tab_quantity').bind('.update-form').bind('.submit-btn').bind('ajax:success', function(){
	$("#items_count").html("<%= @current_cart.items_count %>");
	$("#total_amount").html("<%= number_to_currency_euro @current_cart.sub_total %>");
});
