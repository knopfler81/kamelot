<div class="container">
	<h1>Ajouter le stock</h1>
	
	<div class="row">
		<p><%= @product.title %></p>
		<br><br>


<%# @variants.each do |variant| %>
	<%#= form_with model: @,  html: { class: "update-form", method: :patch, remote: true, }  do |f| %>
		<%#= f.hidden_field :variant_id, value: variant.id %>
			<%#= f.select :variant_id, options_from_collection_for_select(@product.variants, :id, :size), prompt: "Variant", class: 'custom-select', required: true %>
			<%#= f.submit %>
	<%#end %>
<%# end %>

	</div>
	

</div>




		<%#= simple_form_for(@stock, url: admin_stock_path) do |f| %>
			<%#= f.input :quantity %>
			<%#= f.select :variant_id, options_from_collection_for_select(@product.variants, :id, :size), prompt: "Variant", class: 'custom-select', required: true %>
			<%#= f.submit %>
		<%# end %>
