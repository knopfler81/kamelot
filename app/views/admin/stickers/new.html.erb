<div class="container">
	<div class="row">
		<div class="col-12">
			<%= simple_form_for(@sticker, url: admin_product_stickers_path) do |f| %>
				<%= f.hidden_field :product_id, value: @product.id %>
				<%= f.input :quantity %>
				<%= f.submit %>				
			<% end %>
		</div>
	</div>
</div>